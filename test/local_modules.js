const dotenv = require("dotenv");
dotenv.config();

// for api/app.js AND bot/bot.js
// import these modules if working locally

// also run the following to install the module

// npm install dotenv